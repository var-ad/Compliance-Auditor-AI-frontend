# Leave empty for local development; Vite will use its /api proxy.
VITE_API_BASE_URL=

# Production defaults to https://auditor.varad.fyi. Set this in Vercel only
# when you need to override that backend:
# VITE_API_BASE_URL=https://auditor.varad.fyi
